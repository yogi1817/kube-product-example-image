spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
  jpa:
    database: h2
    show-sql: true
    hibernate:
      use-new-id-generator-mappings: false
      ddl-auto: none
